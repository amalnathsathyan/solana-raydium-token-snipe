# TOKEN MINT - ADD LPP - SNIPE

## Setup

1. Install dependencies:
   ```bash
   npm install
2. Update any necessary values inside the scripts as per your requirements.

3. To execute a specific script, run:
    ```bash
    ts-node 'script_name'